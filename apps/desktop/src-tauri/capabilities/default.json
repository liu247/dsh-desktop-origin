{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: the loading page needs the core window/webview permissions; all harness functionality is served over the local dsh web HTTP origin, not through Tauri IPC.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
